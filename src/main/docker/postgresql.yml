version: '2'
services:
  xtrareferralmanager-postgresql:
    image: postgres:12.1
    # volumes:
    #     - ~/volumes/jhipster/xtraReferralManager/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=xtraReferralManager
      - POSTGRES_PASSWORD=
    ports:
      - 5432:5432
